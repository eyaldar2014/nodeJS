// the essence of node is made for the server 
// the essence of js (client) is for the web browser

// different eco system -

// node is entirely contorolled environment compared to client js interface
// node is faster because its has only specif use and expertice. its localStorage(un-dependent)
// node has: "global", "process", "fs...."
// client js has: "window", "document", all dom orders...